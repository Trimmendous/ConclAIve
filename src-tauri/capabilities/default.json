{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "ConclAIve needs core event listening so the frontend can receive council:// streaming events emitted from the agent supervisor threads.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
